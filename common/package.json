{
  "name": "@youreztickets/common",
  "version": "1.0.0",
  "description": "",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
  "clean": "del ./build/*",
  "build": "npm run clean && tsc",
  "pub": "git add . && git commit -m 'publish' && git push && npm run build && npm publish --access public"
},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "del-cli": "^7.0.0",
    "typescript": "^6.0.3"
  }
}
